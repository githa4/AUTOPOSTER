# 🎯 Рейтинг ИИ — UI Функции и Возможности

> Последнее обновление: 21 декабря 2025

Этот документ описывает все UI-функции и возможности системы рейтинга моделей AI.

## 🏆 Полное покрытие Artificial Analysis LEADERBOARD

**117 моделей из официального LEADERBOARD (декабрь 2025)** добавлены во все провайдеры:

### Распределение по провайдерам
- **API2D**: 60 моделей — LLM (33), Vision (7), Code (20)
- **FAL**: 40 моделей — Image (20), Video (20)
- **Kie**: 64 модели — Image (20), Video (20), Music (10), TTS (14)
- **Replicate**: 58 моделей — LLM (13), Code (6), Image (15), Video (13), Music (5), TTS (5)

### Покрытие по категориям
- 📝 **LLM/Text**: 53 модели (ELO 1490-1103) — Gemini 3 Pro, Claude Opus 4.5, Grok-4.1, GPT-5.2, o3, DeepSeek V3.2...
- 🖼️ **Vision**: 7 моделей (ELO 1224-1202) — GPT-4.1, Gemini 2.5, Qwen3 VL, ERNIE 5.0 Vision
- 💻 **Code**: 26 моделей (ELO 1518-1103) — Claude Opus 4.5 Code (1518), GPT-5.2 High (1485)
- 🎨 **Image**: 20 моделей (ELO 1261-1128) — GPT Image 1.5, FLUX.2, Imagen 4, Ideogram 3.0
- 🎬 **Video**: 20 моделей (ELO 1243-1158) — Runway Gen-4.5, Veo 3.1, Kling 2.5, Luma Ray 3, Sora 2
- 🎵 **Music**: 10 моделей (ELO 1109-832) — Suno V4.5, Eleven Music, FUZZ, Lyria 2, Udio
- 🎙️ **TTS**: 14 моделей (ELO 1164-1061) — Inworld TTS, MiniMax Speech, ElevenLabs, OpenAI TTS

---

## 📋 Содержание

1. [Каноническая база моделей](#-каноническая-база-моделей)
2. [Избранное](#-избранное)
3. [Группировка по семействам](#-группировка-по-семействам)
4. [Постепенная загрузка](#-постепенная-загрузка)
5. [Панель деталей модели](#-панель-деталей-модели)
6. [Фильтры и поиск](#-фильтры-и-поиск)
7. [Информация о моделях](#-информация-о-моделях)

---

## 📚 Каноническая база моделей

### Описание
Единый источник правды для классификации AI-моделей. Все модели от разных провайдеров обогащаются данными из канонической базы.

### Структура
**Файл**: `services/modelRating/canonicalModels.ts` (~1700 строк)

**210+ моделей** по категориям:
- **TEXT_MODELS** — ~50 LLM (GPT-4o, Claude 3.5, Gemini 2, Llama 3.3, DeepSeek V3...)
- **IMAGE_MODELS** — 55 text-to-image (Flux 1.1 Pro, DALL-E 3, SD 3.5, Ideogram V2...)
- **VIDEO_MODELS** — 40 text-to-video (Runway Gen-4.5, Veo 3, Kling 2.5, Luma Ray 3...)
- **AUDIO_MODELS** — 50 TTS (Inworld TTS, MiniMax Speech, ElevenLabs, OpenAI TTS...)
- **MUSIC_MODELS** — 11 text-to-music (Suno V4.5, ElevenLabs Music, FUZZ-1.1...)
- **CODE_MODELS** — 3 coding (CodeLlama, Codestral, Qwen Coder)
- **MULTIMODAL_MODELS** — 3 multimodal (LLaVA, Moondream, BLIP-2)

### Что содержит каждая модель
```typescript
{
  id: 'gemini-2.0-flash',              // Канонический ID
  name: 'Gemini 2.0 Flash',            // Название
  category: 'text',                    // Категория
  modality: 'text↔text',               // Модальность
  vendor: 'Google',                    // Вендор
  family: 'Gemini',                    // Семейство
  contextLength: 1000000,              // Контекст
  maxOutputTokens: 8192,               // Макс. выход
  pricing: { inputPerM: 0.075, ... },  // Цены
  providerIds: {                       // Маппинг на провайдеров
    openrouter: 'google/gemini-2.0-flash-exp',
    groq: 'gemini-2.0-flash-exp',
    laozhang: 'gemini-2.0-flash'
  },
  elo?: 1350,                          // ELO из Artificial Analysis
  description?: 'Fast multimodal...'  // Описание
}
```

### Как работает
1. **Провайдер возвращает модели** с их ID (например `google/gemini-2.0-flash-exp` от OpenRouter)
2. **Aggregator ищет в канонической базе** через `findCanonicalByProviderId('openrouter', 'google/gemini-2.0-flash-exp')`
3. **Находит совпадение** → обогащает модель правильными данными (category, vendor, family, elo...)
4. **Если не находит** → оставляет как есть (провайдер сам определяет категорию)

### Преимущества
- ✅ **Единая классификация** — модель всегда в правильной категории
- ✅ **Мультипровайдерность** — одна модель доступна у нескольких провайдеров
- ✅ **ELO рейтинги** — из Artificial Analysis
- ✅ **Актуальные цены** — с источниками (official/community/estimate)
- ✅ **Семейства и вендоры** — автогруппировка

### Утилиты
```typescript
findCanonicalByProviderId('openrouter', 'google/gemini-2.0-flash-exp')
// → Находит модель gemini-2.0-flash

getProviderModelIds('flux-1.1-pro', 'replicate')
// → ['black-forest-labs/flux-1.1-pro']

getModelProviders('flux-1.1-pro')
// → ['openrouter', 'replicate', 'fal', 'together']
```

---

## ⭐ Избранное

### Описание
Система отметки моделей как избранных с сохранением между сессиями.

### Возможности
- **Звёздочка на каждой модели** — первая колонка таблицы
- **Клик по звезде** — добавляет/убирает модель из избранного
- **Кнопка "★" в панели фильтров** — показать только избранные (с счётчиком)
- **Кнопка "⬆ Вверху"** — переключатель сортировки избранных наверх (опционально)
- **Подсветка избранных** — строки избранных моделей слегка подсвечены жёлтым
- **Индикатор в заголовке группы** — ⭐ показывает, что в группе есть избранные модели

### Технические детали
- **Хранение**: localStorage (`LS_FAVORITE_MODELS`)
- **Формат**: массив строк `providerName:modelId`
- **Сортировка**: опциональная через переключатель "Вверху"

### Как пользоваться
1. Нажмите ⭐ напротив нужной модели
2. Модель отметится жёлтой звездой
3. Включите кнопку "⬆ Вверху" если хотите видеть избранные сверху
4. Нажмите кнопку "★" чтобы видеть только избранные

---

## 📁 Группировка по семействам

### Описание
Автоматическая группировка моделей по семействам с возможностью сворачивания.

### Возможности
- **Автогруппировка по бренду** — все модели одного бренда в одной группе
  - GPT (все gpt-*, chatgpt, o1, o3)
  - Claude (все claude-*)
  - Gemini (все gemini-*)
  - Llama (все llama-*, codellama)
  - DeepSeek, Grok, NVIDIA, Mistral, Qwen, Cohere, Phi и другие
  
- **Автогруппировка по совпадениям** — модели с частыми совпадениями в названиях
  - Если >1 модели содержат одно слово/фразу — создаётся группа
  - Только одиночные модели остаются в "Другие"

- **Заголовки групп** — кликабельные строки с:
  - Название семейства
  - Счётчик моделей в группе
  - Стрелка состояния (▼ развёрнуто / ▶ свёрнуто)
  - Индикатор ⭐ если есть избранные модели в группе

- **Кнопки управления**:
  - "Свернуть все" — сворачивает все группы
  - "Развернуть все" — разворачивает все группы

### Технические детали
- **Хранение**: localStorage (`LS_COLLAPSED_GROUPS`)
- **Формат**: объект `{ "GPT": true, "Claude": false, ... }`
- **Алгоритм группировки**:
  1. Проверка на топовые бренды (GPT, Claude, Gemini...)
  2. Подсчёт частот слов/фраз в оставшихся моделях
  3. Создание групп для совпадений >1
  4. Одиночные модели → "Другие"

### Как пользоваться
1. Модели автоматически группируются при загрузке
2. Клик на заголовок группы — свернуть/развернуть
3. Кнопки "Свернуть все" / "Развернуть все" для массового управления

---

## 🔢 Постепенная загрузка

### Описание
Лимит отображения моделей для быстрой загрузки и уменьшения шума.

### Возможности
- **Лимит 20 моделей** — по умолчанию показываются первые 20
- **Кнопка "Показать ещё 20"** — добавляет следующие 20 моделей
- **Кнопка "Показать все (N)"** — показывает сразу весь список
- **Автосброс лимита** — при смене фильтров/поиска/вкладки

### Технические детали
- **Состояние**: `visibleLimit` в компоненте
- **Инкремент**: +20 моделей за раз
- **Счётчик**: футер показывает `Показано X из Y моделей`

### Как пользоваться
1. При открытии вкладки видите топ-20 моделей
2. Если нужно больше — жмите "Показать ещё 20"
3. Или "Показать все" чтобы увидеть весь список сразу

---

## 📱 Панель деталей модели

### Описание
Подробная информация о выбранной модели справа от таблицы (как в OpenRouter).

### Возможности
- **Клик на строку модели** — открывает панель деталей
- **Ресайз мышкой** — тяните за разделитель для изменения ширины
- **Даблклик на разделитель** — сброс ширины до 360px
- **Кнопка X** — закрыть панель

### Отображаемая информация
- 📝 **Название и ID** модели + ссылка на API документацию
- 📖 **Описание** (если доступно у провайдера)
- 📊 **Контекст** — размер контекстного окна (128k, 200k...)
- 🚀 **Макс. выход** — максимум выходных токенов (4k, 16k...)
- 🔄 **Модальность** — вход→выход (text↔text, text→img, img→text...)
- 📅 **Добавлена** — дата добавления модели (Сегодня, 3 дн. назад...)
- 💰 **Цена** — все типы цен:
  - Токены (input/output per 1M)
  - Картинки (per unit)
  - Аудио (per minute)
  - Запросы (fixed per request)
- 🏢 **Провайдер** — информация с предупреждением для неофициальных
- 🔄 **Альтернативные провайдеры** — где ещё доступна эта модель и по какой цене
- 📋 **Сырой JSON** — полные данные модели (свёрнуто по умолчанию)

### Технические детали
- **Хранение ширины**: localStorage (`LS_MODEL_DETAIL_WIDTH`)
- **Минимальная ширина**: 280px
- **Максимальная ширина**: 60% от ширины экрана
- **Адаптивность**: на мобильных панель показывается снизу

### Как пользоваться
1. Кликните на любую строку модели в таблице
2. Справа откроется панель с детальной информацией
3. Тяните за разделитель чтобы изменить ширину панели
4. Даблклик на разделитель для сброса ширины
5. Нажмите X чтобы закрыть панель

---

## 🔍 Фильтры и поиск

### Описание
Система фильтрации и поиска моделей по различным критериям.

### Возможности
- **Поиск по тексту** — поиск по названию модели и ID
- **Фильтр по категории**:
  - Все
  - Текст
  - Код
  - Картинки
  - Видео
  - Аудио
  - Мультимодальные
- **Фильтр "Бесплатные"** — только модели с бесплатным тиром
- **Фильтр "Лучшая цена"** — только модели с лучшей ценой среди провайдеров
- **Фильтр "★ Избранные"** — только избранные модели (с счётчиком)
- **Переключатель "⬆ Вверху"** — сортировка избранных наверх

### Технические детали
- **Поиск**: регистронезависимый, по name и providerModelId
- **Фильтры**: комбинируются (И-логика)
- **Счётчики**: обновляются в реальном времени

### Как пользоваться
1. Введите текст в поле поиска для фильтрации по названию
2. Выберите категорию в кнопках фильтров
3. Включите "Бесплатные" чтобы видеть только free-tier модели
4. Включите "★" чтобы видеть только избранные

---

## 📊 Информация о моделях

### Описание
Данные, отображаемые в таблице моделей.

### Колонки таблицы
| Колонка | Описание | Пример |
|---------|----------|--------|
| ⭐ | Избранное | Звёздочка (жёлтая/серая) |
| Модель | Название и ID | `GPT-4 Turbo` `gpt-4-turbo` |
| Провайдер | Источник модели | `OpenRouter` |
| Тип | Категория | Текст, Картинки, Видео... |
| Контекст | Размер окна | `128k` `200k` |
| Выход | Макс. выход | `4k` `16k` `128k` |
| Модальн. | Модальность | `text↔text` `text→img` |
| Добавлена | Дата | `Сегодня` `3 дн. назад` `Дек 2024` |
| Цена | Стоимость | Input/Output per 1M tokens |

### Бейджи
- 🆓 **FREE** — бесплатная модель
- 💰 **BEST PRICE** — лучшая цена среди провайдеров
- 🆕 **NEW** — новая модель (добавлена недавно)
- 🧠 **REASONING** — reasoning-модель
- 📖 **OPEN** — open-source модель
- 🔒 **PROP** — проприетарная модель

### Сортировка
- По умолчанию: по дате добавления (новые сверху)
- Избранные: опционально сверху (при включённой кнопке "⬆ Вверху")
- Внутри группы: по умолчанию по дате, избранные опционально сверху

---

## 🎯 Лучшие практики использования

### Для быстрого поиска нужной модели:
1. Используйте текстовый поиск
2. Выберите категорию (Текст/Картинки/Видео...)
3. Включите "Бесплатные" если нужен free-tier
4. Добавьте в избранное часто используемые модели

### Для сравнения моделей:
1. Откройте панель деталей первой модели
2. Изучите характеристики
3. Кликните на другую модель для сравнения
4. Используйте колонку "Альтернативные провайдеры" для поиска лучшей цены

### Для работы с большим списком:
1. Сверните ненужные группы семейств
2. Используйте постепенную загрузку (показывать по 20)
3. Отмечайте интересные модели в избранное
4. Включите "⬆ Вверху" чтобы видеть избранные сверху

---

## 🛠️ Технические ограничения

- **Максимум моделей в таблице**: без лимитов (но рекомендуется использовать постепенную загрузку)
- **Кэш данных провайдеров**: 1-12 часов в зависимости от провайдера
- **localStorage лимиты**: ~5-10MB (достаточно для избранного и настроек)
- **Панель деталей**: минимальная ширина 280px, максимальная 60% экрана

---

## 🔄 Обновление данных

- **Автообновление**: каждые N часов (зависит от провайдера)
- **Ручное обновление**: кнопка "Обновить" (иконка ⟳) в панели
- **Очистка кэша**: в настройках провайдеров

---

## 🔄 ПЛАН: Унификация системы выбора моделей (v2)

> **Статус**: В работе  
> **Дата**: 20 декабря 2025  
> **Обновлено**: Уточнённый план с двусторонней синхронизацией

### Ключевые решения

1. ✅ **ModelAssignmentRow НЕ ТРОГАЕМ** — схема назначения остаётся
2. ✅ **Таблица моделей под каждым API** — только модели этого провайдера
3. ✅ **Двусторонняя синхронизация** — изменения в таблице ↔ назначения
4. ✅ **Quick Assign в таблице** — кнопки Текст/YouTube/Картинки

### Схема UI (SettingsPage)

```
┌─────────────────────────────────────────────────────────┐
│  API Wallet: Gemini                                     │
│  ├── Ключи: [key1] [key2] [+ Добавить]                 │
│  └── Кнопка: [Загрузить модели]                        │
├─────────────────────────────────────────────────────────┤
│  📋 ТАБЛИЦА МОДЕЛЕЙ GEMINI (новая!)                    │
│  ┌─────────────────────────────────────────────────────┐
│  │ ⭐ | Модель           | Тип  | Цена | Назначить    │
│  │────┼──────────────────┼──────┼──────┼──────────────│
│  │ ☆  | Gemini 3 Flash   | TEXT | $0.1 | 📝 🎬 🖼️     │
│  │ ★  | Gemini 3 Pro     | TEXT | $0.5 | 📝 🎬 🖼️     │
│  │ ☆  | Gemini 2.5 Image | IMG  | $1.0 |       🖼️     │
│  └─────────────────────────────────────────────────────┘
│  Клик на 📝 → модель = textModel                       │
│  Клик на 🎬 → модель = youtubeModel                    │
│  Клик на 🖼️ → модель = imageModel                      │
├─────────────────────────────────────────────────────────┤
│  ... другие API (OpenRouter, Kie, Replicate) ...       │
├─────────────────────────────────────────────────────────┤
│  📌 НАЗНАЧЕНИЕ МОДЕЛЕЙ (существующий, не трогаем!)     │
│  ┌─────────────────────────────────────────────────────┐
│  │ Текст:    [Gemini 3 Pro    ▼] ← синхронизирован    │
│  │ YouTube:  [Gemini 3 Flash  ▼] ← синхронизирован    │
│  │ Картинки: [Gemini 2.5 Img  ▼] ← синхронизирован    │
│  └─────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────┘
```

### Текущая проблема

Сейчас в проекте **3 разных системы выбора моделей**:

| # | Место | Файл | Проблема |
|---|-------|------|----------|
| 1 | SettingsPage → ModelAssignmentRow | SettingsPage.tsx | Основной выбор |
| 2 | ModelManagerModal | ModelManager.tsx | Только просмотр, нет выбора |
| 3 | MultiProviderExplorer | benchmarks/MultiProviderExplorer.tsx | Своё избранное, не связано |

**Дублирование избранного**: 2 хранилища (`autopost_favorite_models` и `autopost_favorite_benchmark_models`)

---

### ЭТАП 1: Подготовка

1. **Унифицировать хранилище избранного**
   - Удалить `autopost_favorite_benchmark_models` из MultiProviderExplorer
   - Использовать только `AppContext.favoriteModelIds` везде

2. **Определить единый тип модели**
   - Проверить совместимость `Model` (types.ts) и `AggregatedModel` (modelRating)
   - При необходимости привести к одному формату

---

### ЭТАП 2: Создание универсального компонента

3. **Создать `components/ModelsTable.tsx`**
   - Вынести логику таблицы из MultiProviderExplorer
   - Пропсы: `provider`, `mode` (inline/fullpage), `showAssignButtons`
   - Использовать AppContext для избранного

4. **Добавить Quick Assign кнопки**
   - В каждой строке: иконки "Текст / YouTube / Картинки"
   - При клике — назначает модель в соответствующий слот

---

### ЭТАП 3: Интеграция в Settings

5. **Встроить ModelsTable под каждый ApiWallet**
   - Gemini → показывает только модели gemini
   - OpenRouter → только openrouter
   - Kie → только kie
   - Replicate → только replicate

6. **Связать с ModelAssignmentRow (двусторонняя синхронизация)**
   - При клике Quick Assign → обновляется `modelConfig` в AppContext
   - ModelAssignmentRow читает из того же `modelConfig`
   - **Изменения в ModelAssignmentRow** → таблица подсвечивает назначенную модель
   - **Изменения в таблице** → ModelAssignmentRow обновляется автоматически

---

### ЭТАП 4: Рефактор Benchmarks

7. **Заменить встроенную таблицу на ModelsTable**
   - Режим `mode="fullpage"`
   - `provider="all"` — все провайдеры
   - Без Quick Assign (только просмотр + избранное)

8. **Удалить дублирующий код**
   - Локальный стейт favoriteIds → AppContext
   - Локальный selectedModel → можно оставить для детали

---

### ЭТАП 5: Очистка

9. **Удалить ModelManagerModal**
   - Файл `components/ModelManager.tsx`
   - Все импорты и использования в SettingsPage

10. **Удалить устаревшие хранилища**
    - `LS_FAVORITE_MODELS` в MultiProviderExplorer
    - Старый код фильтрации в ModelManager

---

### ЭТАП 6: Финализация

11. **Проверить типы и сборку**
    - `npm run build` без ошибок
    - Проверить все импорты

12. **Тестирование флоу**
    - Settings → API → Модели → Избранное → Назначение
    - Benchmarks → Просмотр → Избранное синхронизировано

---

### Итоговая структура файлов

```
components/
  ├── ModelsTable.tsx        ← НОВЫЙ (универсальная таблица)
  ├── SettingsPage.tsx       ← использует ModelsTable inline
  └── benchmarks/
        └── MultiProviderExplorer.tsx  ← использует ModelsTable fullpage

УДАЛИТЬ:
  └── ModelManager.tsx       ← больше не нужен
```

---

### Ожидаемый результат

| До | После |
|----|-------|
| 3 места выбора моделей | 1 компонент, 2 режима |
| 2 хранилища избранного | 1 в AppContext |
| Попап ModelManager | Удалён |
| Одностороннее назначение | Двусторонняя синхронизация |
| Разрозненный код | Единая логика |

---

## 📝 История обновлений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2025-12-20 | 1.2 | План v2: двусторонняя синхронизация, схема UI, уточнения |
| 2025-12-20 | 1.1 | Добавлен план унификации выбора моделей |
| 2025-12-20 | 1.0 | Первая версия документации |

---

**Авторы**: AutoPost.ai Team  
**Лицензия**: Внутренняя документация проекта
