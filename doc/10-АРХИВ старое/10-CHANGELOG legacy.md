# 10-CHANGELOG legacy

Статус: 🔒 Заморожен
Приоритет: ⭐ Желательно

---

## История правок

| Дата | Время | Что сделано |
|------|-------|-------------|
| 2025-12-18 | 08:29 | Перенесён в архив. Канонический `CHANGELOG.md` теперь в корне проекта. |

---

## Суть

Это **исторический (legacy) changelog**, сохранённый для справки.
Новые изменения проекта фиксируются **только** в корневом `CHANGELOG.md`.

---

## Legacy содержимое

# 📜 История изменений (Changelog) - AutoPost.ai

Этот документ отражает ключевые этапы разработки проекта, внедренные функции и технические решения.

---

## [v0.5.1] - Hotfix & Planning (Текущая версия)
**Дата:** Текущая сессия

### 🐛 Исправлено (Fixed)
1.  **Персистентность авторизации (Auth Persistence):**
    *   **Проблема:** При обновлении страницы (`F5`) происходил сброс состояния пользователя (`user: null`), что вызывало "мигание" интерфейса или выброс на экран логина до момента ответа от сервера Supabase.
    *   **Решение:** В `AppContext.tsx` изменена инициализация `useState`. Теперь состояние считывается из `localStorage` (`autopost_cached_user`) мгновенно при старте приложения.
    *   **Результат:** Стабильная сессия без ложных разлогиниваний.

### 📝 Документация
2.  **План развития (Roadmap):**
    *   Создан файл `doc/План развития.md`.
    *   Определена стратегия перехода на Serverless (Supabase Edge Functions) для скрытия API ключей.
    *   Запланированы спринты: Планировщик постов (cron), Календарь, интеграция WordPress/VK.

---

## [v0.5.0] - Cloud Sync & Supabase Integration
**Статус:** Внедрено

### 🚀 Добавлено (Added)
1.  **Бэкенд (Supabase):**
    *   Подключена библиотека `@supabase/supabase-js`.
    *   Создана схема БД:
        *   `profiles`: Хранение настроек (JSONB) и API ключей.
        *   `integrations`: Хранение подключенных каналов (Telegram bot tokens).
2.  **Аутентификация:**
    *   Реализован `AuthModal.tsx` (Email + Password).
    *   Настроена RLS (Row Level Security) — пользователи видят только свои данные.
3.  **Синхронизация:**
    *   Двусторонняя синхронизация настроек (Local <-> Cloud). Если пользователь логинится, локальные настройки отправляются в облако.
4.  **Инструменты отладки:**
    *   Добавлен **Cloud Inspector** в настройки — модальное окно для просмотра сырых JSON данных, хранящихся в базе.

---

## [v0.4.5] - Replicate & Image Generation Upgrade
**Статус:** Внедрено

### ✨ Улучшения (Changed)
1.  **Генерация изображений:**
    *   Интегрирован провайдер **Replicate**.
    *   Добавлена поддержка моделей: `Flux.1 Schnell`, `Flux.1 Dev`, `SDXL`, `Recraft V3`.
    *   Реализован механизм проксирования запросов через `corsproxy.io` (временное решение для Client-Side).
2.  **AI Tools для изображений:**
    *   Добавлен `Upscale` (улучшение качества).
    *   Добавлен `Remove Background` (удаление фона).

---

## [v0.4.0] - Multi-Provider Architecture
**Статус:** Внедрено

### 🚀 Добавлено (Added)
1.  **Поддержка альтернативных LLM:**
    *   Добавлен провайдер **OpenRouter** (доступ к Claude 3.5, GPT-4).
    *   Добавлен провайдер **Kie.ai** (доступ к DeepSeek R1).
2.  **Настройки (SettingsPage):**
    *   Полностью переписан интерфейс настроек.
    *   Внедрена система вкладок (Tabs) для каждого провайдера.
    *   Добавлена возможность тестирования подключения (`Test Connection`) с замером латентности.

---

## [v0.3.0] - UI Redesign ("IDE Style")
**Статус:** Внедрено

### 🎨 Дизайн (UI/UX)
1.  **Концепция:**
    *   Интерфейс переработан в стиле VS Code / IDE.
    *   **Activity Bar:** Узкая панель слева для навигации.
    *   **Sidebar:** Проводник файлов (история черновиков и папки проектов).
    *   **Terminal Panel:** Нижняя панель для логов генерации и квот.
2.  **Command Palette:**
    *   Реализовано меню команд (`Ctrl+K`) для быстрого перехода по файлам и функциям.

---

## [v0.2.0] - Core Features (Telegram & Gemini)
**Статус:** Внедрено

### ⚙️ Функционал
1.  **Сервисный слой:**
    *   `geminiService.ts`: Логика общения с Google Gemini API (генерация текста и JSON).
    *   `telegramService.ts`: Отправка сообщений и фото (FormData) напрямую в Telegram Bot API.
2.  **Редактор:**
    *   Поддержка HTML тегов Telegram (`<b>`, `<i>`, `<code>`, `<tg-spoiler>`).
    *   "Magic Tools": AI-рерайт выделенного текста.
3.  **YouTube Mode:**
    *   Добавлен режим суммаризации видео по ссылке (использует Gemini 1.5/Pro с большим контекстом).

---

## [v0.1.0] - MVP (Project Start)
**Статус:** Архив

*   Инициализация React + Vite + TypeScript.
*   Базовая интеграция Google Gemini (`generateContent`).
*   Простой UI на TailwindCSS.
*   Локальное хранение истории в `localStorage`.
