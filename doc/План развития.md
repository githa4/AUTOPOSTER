# 🚀 AutoPost.ai: План развития и технические рекомендации (Roadmap v1.0)

## 1. Архитектура и Безопасность (Критический приоритет)

Текущая реализация хранит API ключи в браузере и базе данных, но использует их на клиенте. Это допустимо для MVP, но рискованно для продакшна.

### **1.1. Переход на Supabase Edge Functions**
Вместо того чтобы отправлять запросы в Telegram/Replicate/OpenRouter прямо из браузера пользователя, нужно создать серверные функции.
*   **Зачем:**
    *   **Скрытие токенов:** Пользователь никогда не увидит ваш Replicate API Key или Telegram Bot Token в Network Tab браузера.
    *   **Обход CORS:** Больше не нужны публичные `corsproxy.io` (это небезопасно и нестабильно).
    *   **Facebook/Instagram:** API Meta *требует* серверной части для OAuth и публикации.
*   **Реализация:**
    Создать функции: `publish-telegram`, `generate-image`, `generate-text`. Клиент вызывает их через `supabase.functions.invoke()`.

### **1.2. Шифрование ключей в БД**
Сейчас ключи лежат в JSON поле `settings` в открытом виде.
*   **Рекомендация:** Использовать Supabase Vault или шифровать поля перед записью в БД, расшифровывая их только внутри Edge Functions.

---

## 2. Расширение Интеграций (Scaling)

Текущая версия поддерживает только Telegram. Для полноценного SMM-комбайна нужны другие платформы.

### **2.1. WordPress (REST API)**
*   **Сложность:** Низкая.
*   **Функционал:** Публикация статей (Long Reads), которые генерируются в режиме "Instant View".
*   **Требование:** Поля для ввода URL сайта, Username и Application Password (в настройках интеграции).

### **2.2. VKontakte (VK) & Одноклассники**
*   **Сложность:** Средняя.
*   **Функционал:** Кросспостинг для RU-сегмента.
*   **Требование:** Получение `access_token` через Implicit Flow или Server-Side OAuth.

### **2.3. Планировщик постов (Scheduling)**
Это самая ожидаемая функция для SMM.
*   **Реализация:**
    1.  Добавить поле `scheduled_at` (timestamp) в таблицу `posts`.
    2.  Использовать **pg_cron** (плагин внутри Supabase), который раз в минуту запускает SQL-запрос или Edge Function.
    3.  Функция ищет посты, где `status = 'scheduled'` и `scheduled_at <= now()`, и публикует их.

---

## 3. Улучшение AI возможностей

### **3.1. "Память" и Контекст канала**
Сейчас каждый пост генерируется в вакууме.
*   **Идея:** Загрузить последние 10 постов из канала (через Telegram API `getHistory`, если возможно, или сохранять историю генераций).
*   **Реализация:** При генерации нового поста скармливать AI "стиль и тон" предыдущих успешных постов. Это называется RAG (Retrieval Augmented Generation) — но в мини-формате.

### **3.2. SEO-анализатор (для Long Reads)**
*   **Функционал:** Перед генерацией статьи AI сначала ищет ключевые слова (через Google Search Tool, который уже подключен к Gemini) и составляет структуру статьи на их основе.

### **3.3. Редактор Изображений (Canvas)**
Сейчас картинка либо генерируется, либо загружается.
*   **Идея:** Добавить простой редактор (как Canva на минималках).
*   **Функционал:** Наложить текст (заголовок) на картинку, добавить логотип канала, сделать цветокоррекцию перед постингом.

---

## 4. UI/UX Улучшения (Интерфейс)

### **4.1. Календарный вид (Calendar View)**
Вместо списка черновиков слева — полноценный календарь.
*   **Польза:** Наглядно видеть контент-план на месяц.
*   **Drag & Drop:** Перетаскивание поста с одной даты на другую для изменения расписания.

### **4.2. Мобильная адаптация**
Текущий интерфейс ("VS Code style") сложен для телефона.
*   **Решение:** Для мобильных устройств скрывать Sidebar и Preview, оставляя только Chat/Editor, и переключаться табами.

### **4.3. Командная работа (Teamspaces)**
*   **Сейчас:** Данные привязаны к `user_id`.
*   **Будущее:** Создать таблицу `teams` и `team_members`. Пользователи могут приглашать коллег в проекты.
*   **Роли:** Администратор (настраивает ключи), Редактор (генерирует), Вьюер (только смотрит).

---

## 5. Монетизация (Если планируется SaaS)

Если вы решите продавать доступ к сервису:

1.  **Система кредитов:** Вместо ввода своих API ключей, пользователь покупает "внутренние токены". Вы используете свой корпоративный ключ Gemini/Replicate, а с пользователя списываете баланс.
2.  **Stripe Integration:** Подключить Stripe к Supabase для обработки подписок.

---

## Резюме: Что делать прямо сейчас?

Я рекомендую следующий порядок действий (спринты):

1.  **Спринт 1 (Backend):** Настройка Supabase Edge Functions для Telegram публикации. Это уберет ключи с клиента и повысит безопасность.
2.  **Спринт 2 (Feature):** Реализация **Планировщика (Scheduling)**. Это киллер-фича.
3.  **Спринт 3 (Integration):** Добавление WordPress или VK.
4.  **Спринт 4 (UI):** Внедрение календаря контента.