# 03-ROADMAP план развития

Статус: 🚧 В работе
Приоритет: 🔥 Критично

---

## История правок

| Дата | Время | Что сделано |
|------|-------|-------------|
| 2025-12-18 | 08:29 | Приведён к стандарту `03🔵-ROADMAP.md`, перенесено содержимое из `doc/План развития.md`. |

---

## Суть

План развития AutoPost.ai: приоритеты по безопасности/архитектуре, интеграциям, UI и монетизации.

---

## Содержание

### 🚧 В работе
- 1) Архитектура и безопасность (переход на server-side вызовы)

### ⏳ Планируется
- 2) Планировщик постов (Scheduling)
- 3) Новые интеграции (WordPress/VK и др.)
- 4) Улучшения UI/UX (календарь, mobile)

### ✅ Выполнено
- —

---

## Детали (Roadmap v1.0)

## 1. Архитектура и Безопасность (Критический приоритет)

Текущая реализация хранит API ключи в браузере и базе данных, но использует их на клиенте. Это допустимо для MVP, но рискованно для продакшна.

### 1.1. Переход на Supabase Edge Functions
Вместо того чтобы отправлять запросы в Telegram/Replicate/OpenRouter прямо из браузера пользователя, нужно создать серверные функции.
- Зачем:
  - Скрытие токенов: пользователь не увидит ключи/токены в Network.
  - Обход CORS: без публичных прокси.
  - Facebook/Instagram: потребуется серверная часть для OAuth и публикаций.
- Реализация:
  - Функции: `publish-telegram`, `generate-image`, `generate-text`.
  - Клиент вызывает через `supabase.functions.invoke()`.

### 1.2. Шифрование ключей в БД
Сейчас ключи лежат в JSON поле `settings` в открытом виде.
- Рекомендация: Supabase Vault или шифрование полей перед записью; расшифровка — только внутри Edge Functions.

---

## 2. Расширение интеграций (Scaling)

### 2.1. WordPress (REST API)
- Сложность: низкая
- Функционал: публикация Long Reads (Instant View)
- Требование: URL сайта, Username, Application Password

### 2.2. VKontakte (VK) и Одноклассники
- Сложность: средняя
- Функционал: кросспостинг
- Требование: `access_token` через Implicit Flow или server-side OAuth

### 2.3. Планировщик постов (Scheduling)
- Реализация:
  1) Добавить `scheduled_at` (timestamp) в таблицу `posts`
  2) Использовать `pg_cron` (в Supabase) — запуск раз в минуту SQL/Edge Function
  3) Функция публикует посты, где `status = 'scheduled'` и `scheduled_at <= now()`

---

## 3. Улучшение AI возможностей

### 3.1. Память и контекст канала
- Идея: учитывать последние 10 успешных постов (mini-RAG)

### 3.2. SEO-анализатор (для Long Reads)
- Идея: перед генерацией статьи — сбор ключей и структуры

### 3.3. Редактор изображений (Canvas)
- Идея: текст/логотип/цветокоррекция перед публикацией

---

## 4. UI/UX улучшения

### 4.1. Календарный вид
- Календарь на месяц + drag&drop постов

### 4.2. Мобильная адаптация
- На мобильных скрывать Sidebar/Preview; переключение табами

### 4.3. Командная работа (Teamspaces)
- Таблицы `teams` и `team_members`, роли: Admin/Editor/Viewer

---

## 5. Монетизация (если планируется SaaS)

- Система кредитов (внутренние токены)
- Stripe + Supabase для подписок

---

## Резюме: что делать сейчас (спринты)

1) Спринт 1: Supabase Edge Functions для Telegram публикации
2) Спринт 2: Планировщик (Scheduling)
3) Спринт 3: Интеграции (WordPress или VK)
4) Спринт 4: UI календарь
