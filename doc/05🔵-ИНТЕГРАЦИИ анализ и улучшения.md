# 05🔵-ИНТЕГРАЦИИ — анализ и план улучшений

Статус: 🚧 В работе
Приоритет: 🔥 Критично

---

## Контекст
Раздел «Интеграции» в приложении фактически объединяет два разных домена:

1) **AI-провайдеры** (Google Gemini / Kie.ai / OpenRouter / Replicate)
   - хранение API ключей
   - синхронизация списков моделей
   - назначение «модель → роль» (Текст / YouTube / Изображение)

2) **Каналы публикации (Destinations)** (сейчас в UI: Telegram)
   - хранение токенов/каналов
   - тест соединения
   - включение/выключение

Цель: довести этот раздел до состояния, когда **выбор провайдера + ключа + модели** делается быстро и понятно, без «подводных камней», а ошибки не заставляют пользователя гадать.

---

## Где в коде
- UI раздела «Интеграции»: `components/SettingsPage.tsx`
- Модальный «Центр моделей»: `components/ModelManager.tsx`
- Хранилище состояния/синхронизация: `context/AppContext.tsx`
- Модели/тесты/получение списков: `services/geminiService.ts`, `services/replicateService.ts`, `services/telegramService.ts`
- Локализация: `locales.ts`

---

## Визуальный (UX) анализ — проблемы и нелогичности

### 1) Смешение терминов «Интеграции»
Сайдбар и заголовки используют слово «Интеграции», но внутри это одновременно:
- кошелёк API ключей (AI)
- назначения моделей (AI)
- каналы публикации (Destinations)

Из-за этого пользователь не понимает: «я сейчас настраиваю ИИ или публикацию?».

Рекомендация:
- Развести названия и визуальные секции:
  - **AI провайдеры** (ключи + модели)
  - **Каналы** / **Назначения** (публикация)

### 2) “Лимиты и квоты (N)” отображается как счётчик ключей
В UI счётчик ключей подписан «Лимиты и квоты», но число фактически = количество ключей.

Рекомендация:
- Переименовать на «Ключи (N)».

### 3) Тесты и ошибки
Сейчас часть ошибок может показываться «сырой» строкой (зависит от места).

Рекомендация:
- Для пользователя: показывать локализованное сообщение (например: «Тест не пройден»).
- Для разработчика: подробности — в `console.error(...)`.

### 4) Replicate: “Синхронизация моделей” требует ключ
В текущей логике список моделей Replicate в UI в основном статический/локальный,
но кнопка синхронизации запрашивает ключ.

Рекомендация:
- Либо разрешить показывать статический список без ключа,
- либо явно объяснить, что ключ нужен не для списка, а для тестовой генерации.

### 5) Назначение моделей (Model Assignments)
Плюсы:
- Есть поиск по моделям и избранные
- Есть карточка деталей и тест

Минусы:
- Нет быстрых подсказок по “какая модель лучше для роли”
- Нет явной индикации, что для выбранного провайдера **нет активного ключа**

Рекомендация:
- В селекте провайдера: если нет активного ключа — показывать предупреждение/disabled.
- В селекте моделей: если список пуст — предложить «Добавьте ключ → Обновить модели».

---

## Технический анализ — критичные проблемы (P0)

### P0-1) Интеграции не сохранялись в localStorage
`integrations` и `telegramConfig` читаются из localStorage при старте, но ранее не записывались обратно.
Итог: после перезагрузки данные могли потеряться.

Решение: добавить `useEffect`-персист.

### P0-2) Рассинхрон id интеграции между UI и Supabase
Ранее в Supabase вставка шла без `id`, а в UI использовался случайный uuid.
Удаление шло по UI-id — в базе запись не находилась.

Решение: при insert всегда передавать `id`.

### P0-3) Cloud Viewer показывал интеграции всех пользователей
В Cloud Viewer делался `select('*')` по таблице integrations без фильтра по `user_id`.
Это потенциальная утечка данных (даже при RLS лучше не провоцировать).

Решение: фильтровать `.eq('user_id', userId)`.

---

## Сделано в коде (в рамках этой ревизии)
- Добавлен персист `telegramConfig` и `integrations` в localStorage.
- Добавлена загрузка интеграций из Supabase при логине.
- Исправлен insert/delete интеграций в Supabase: одинаковый `id` и фильтр по `user_id`.
- Cloud Viewer теперь читает integrations только текущего пользователя.
- Исправлена визуальная ошибка в сайдбаре: `border-l-text-*` → корректный border-class.
- Исправлена подпись счётчика ключей: «Ключи (N)».

---

## Рекомендации по доработке (план)

### P1 (сильно улучшит удобство)
1) **Декларативная модель состояний** для провайдеров:
   - `hasActiveKey`
   - `modelsState: idle/loading/ready/error`
   - `lastSyncAt`

2) **Обучающие подсказки в пустых состояниях**
   - Если нет ключа → CTA: «Добавить ключ»
   - Если нет моделей → CTA: «Обновить модели»

3) **Карта ролей моделей**
   - Для ролей (Текст/YouTube/Изображение) показывать рекомендованные варианты (например по избранным)
   - Автоподбор первого валидного варианта, если текущий id отсутствует

4) **Единый подход к ошибкам**
   - UI: локализованный short-message
   - console: подробности

### P2 (качество кода/поддерживаемость)
1) Разбить `SettingsPage.tsx` на подкомпоненты по доменам:
   - `ApiWallet`, `ProviderTab`, `ModelAssignments`, `DestinationsTab`

2) Убрать `any` в местах:
   - `integrationsData`, `profileData`, `ModelAssignmentRow props`, `SearchableSelect`

3) Убрать неиспользуемые/наследные компоненты (или пометить как legacy)
   - `SettingsModal.tsx` (если точно не используется)

---

## Чеклист ручной проверки
- Добавление ключа → появляется в списке, помечается активным при первом добавлении.
- Смена активного ключа → используется в тестах.
- Обновление моделей:
  - Gemini/Kie/OpenRouter: после обновления модели появляются в «Центре моделей» и в селектах.
  - Replicate: статический список виден.
- Назначение моделей сохраняется после перезагрузки.
- Добавление Telegram интеграции сохраняется после перезагрузки.
- Удаление интеграции удаляет и локально, и в облаке (при логине).
- Cloud Viewer показывает только данные текущего пользователя.

---

## Технические заметки (UI)

### Tailwind: миграция с CDN на сборку
Причина: браузерное предупреждение `cdn.tailwindcss.com should not be used in production`.

Сделано:
- Удалён Tailwind CDN из `index.html`.
- Tailwind собирается через PostCSS (Tailwind v4): `postcss.config.cjs`, `tailwind.config.cjs`.
- `index.css` использует синтаксис Tailwind v4: `@import "tailwindcss";`.
- CSS подключается через entry (`index.tsx`), а не через `<link>`.

Если после смены Tailwind «поплыл дизайн», почти всегда причина в том, что utilities не сгенерировались/не подключились.
Проверка: в production build должен появляться `dist/assets/*.css` ненулевого размера.

### Известная проблема: React warning про useEffect deps
Иногда в консоли может появляться:
`The final argument passed to useEffect changed size between renders.`

Обычно это означает, что где-то deps формируются переменной длины (например, `useEffect(fn, someArray)` или через spread `...`).
Чтобы исправить точно и быстро, нужен component stack из консоли (5–10 строк под warning), чтобы найти точный файл/хук.
